{
  "name": "grounded-analyst",
  "private": true,
  "version": "0.1.0",
  "description": "Grounded — an AI data analyst that runs real Python in an isolated sandbox: upload a dataset, ask in plain English, get a grounded answer, chart, table, and the generated code. By BIS.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:core": "npm run build --workspace @grounded/core",
    "dev": "npm run dev --workspace web",
    "lint": "npm run lint --workspace web",
    "typecheck": "npm run build --workspace @grounded/core && npm run typecheck --workspace web && npm run typecheck --workspace mcp && npm run typecheck --workspace @grounded/eval",
    "test": "npm test --workspace @grounded/core",
    "eval": "npm run eval --workspace @grounded/eval",
    "mcp": "npm run start --workspace mcp",
    "cli:analyze": "tsx packages/core/src/cli/analyze.ts",
    "cli:worker": "tsx packages/core/src/cli/worker.ts"
  },
  "engines": {
    "node": ">=20"
  }
}
