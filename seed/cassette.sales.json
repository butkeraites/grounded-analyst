{
  "generateCode:27ad5735390195c4f8aedea5b456f5b222a2bc94": "region_rev = df.groupby('region')['revenue'].sum().sort_values(ascending=False)\ntop_region = region_rev.idxmax()\nprint(\"Total revenue by region:\")\nprint(region_rev.to_string())\nprint(f\"\\nTop region: {top_region} (${region_rev.max():,.0f})\")\n\nbreakdown = df.pivot_table(index='region', columns='product', values='revenue', aggfunc='sum')\nprint(\"\\nRevenue by region x product:\")\nprint(breakdown.to_string())\n\ntop_products = (\n    df[df['region'] == top_region]\n    .groupby('product')['revenue'].sum()\n    .sort_values(ascending=False)\n)\nprint(f\"\\n{top_region} breakdown by product:\")\nprint(top_products.to_string())\nlead_product = top_products.idxmax()\nshare = top_products.max() / top_products.sum()\nprint(f\"\\nIn {top_region}, {lead_product} leads with {share:.0%} of the region's revenue.\")\n\nax = breakdown.plot(kind='bar', figsize=(7, 4))\nax.set_ylabel('Revenue ($)')\nax.set_xlabel('Region')\nax.set_title('Revenue by region and product')\n\nresult = breakdown.reset_index()\n",
  "interpret:f531d1c1166966aeaa63bb71b19eaf50380457ee": "North is the top-revenue region at $26,990, ahead of South's $21,800 — about a 24% lead. Within North, the two products are close but Gadget is the larger contributor at $14,840 (55% of the region's revenue) versus Widget's $12,150. The same ordering holds in South (Gadget $11,900 vs Widget $9,900), so Gadget outsells Widget in both regions; North's edge comes from higher volume across both lines rather than from a different product mix.\n",
  "generateCode:cb9b952b7e4d7f97b4bf9a4bfd4a8cbead0fdd4d": "month_order = ['Jan', 'Feb', 'Mar']\ndf['month'] = pd.Categorical(df['month'], categories=month_order, ordered=True)\nmonthly = df.pivot_table(index='month', columns='region', values='revenue', aggfunc='sum', observed=True)\nprint(\"Monthly revenue by region:\")\nprint(monthly.to_string())\n\ngrowth = (monthly.iloc[-1] / monthly.iloc[0] - 1) * 100\nprint(\"\\nJan -> Mar growth (%):\")\nprint(growth.round(1).to_string())\n\nax = monthly.plot(marker='o', figsize=(7, 4))\nax.set_ylabel('Revenue ($)')\nax.set_xlabel('Month')\nax.set_title('Monthly revenue: North vs South')\n\nresult = monthly.reset_index()\n",
  "interpret:31a47b2294e7b6fa55d7eee538599674e2a83524": "Both regions grow every month, but the trend differs in pace. North is larger in absolute terms throughout — $7,800 → $9,090 → $10,100 from January to March — while South runs lower at $5,850 → $7,150 → $8,800. The gap narrows, though: South grew 50.4% over the quarter versus North's 29.5%, so South is catching up even as North stays ahead. If those slopes hold, South's faster momentum would keep closing the distance in the following months.\n",
  "generateCode:ca4b085a89fdc93def7659ffae6c988cf811efa5": "by_product = df.groupby('product')['revenue'].sum().sort_values(ascending=False)\nprint(\"Total revenue by product:\")\nprint(by_product.to_string())\nleader = by_product.idxmax()\nshare = by_product.max() / by_product.sum()\nprint(f\"\\n{leader} leads overall with ${by_product.max():,.0f} ({share:.1%} of total revenue).\")\n\nby_region = df.pivot_table(index='product', columns='region', values='revenue', aggfunc='sum')\nprint(\"\\nRevenue by product and region:\")\nprint(by_region.to_string())\n\nax = by_product.plot(kind='bar', figsize=(6, 4), color=['#c2410c', '#1a1a1a'])\nax.set_ylabel('Revenue ($)')\nax.set_xlabel('Product')\nax.set_title('Total revenue by product')\n\nresult = by_region.reset_index()\n",
  "interpret:e58464c99feb3f77c9a6a07c01b4023ed6d1c37c": "Gadget is the bigger earner overall, at $26,740 versus Widget's $22,050 — about 54.8% of total revenue against 45.2%. The lead isn't just a regional quirk: Gadget outsells Widget in both regions (North $14,840 vs $12,150, South $11,900 vs $9,900), so its edge is consistent rather than driven by one strong market. Widget stays a solid second everywhere, but Gadget is the clear revenue driver across the board.\n"
}
