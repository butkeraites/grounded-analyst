{
  "name": "@grounded/eval",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Analyst eval harness: execution-based correctness + grounding faithfulness over a golden set, as a CI regression gate.",
  "scripts": {
    "eval": "tsx src/run.ts",
    "test": "node --import tsx --test \"src/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@grounded/core": "*"
  },
  "devDependencies": {
    "tsx": "^4.23.13",
    "typescript": "^7.0.2"
  }
}
