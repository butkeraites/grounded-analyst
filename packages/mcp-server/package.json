{
  "name": "@julius/mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "The MCP tool surface for the Julius clone — one set of tools over the one core, shared by the stdio server and the HTTP route.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./http": "./src/http.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "@julius/core": "*",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "tsx": "^4.19.2"
  }
}
