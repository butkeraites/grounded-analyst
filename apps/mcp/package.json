{
  "name": "mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server exposing the Julius clone's capability surface. Milestone 1 of it: the LLM-worker tools, so an MCP host (Claude) powers the platform's model over the bridge.",
  "scripts": {
    "start": "tsx src/server.ts",
    "agent": "tsx src/example-agent.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@julius/core": "*",
    "@julius/mcp-server": "*",
    "@modelcontextprotocol/sdk": "^1.13.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^7.0.2"
  }
}
